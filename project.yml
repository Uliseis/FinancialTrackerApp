name: OdysseyFinance
options:
  bundleIdPrefix: com.uliseis
  deploymentTarget:
    iOS: "26.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true
  groupSortPosition: top
settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: minimal
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    # Apple Developer Team. Simulator builds work without signing; device builds +
    # CloudKit provisioning require Xcode to be signed into this team's Apple ID.
    DEVELOPMENT_TEAM: "Q35DSRH5L8"
    CODE_SIGN_STYLE: Automatic

packages:
  Core:
    path: Core

targets:
  OdysseyFinance:
    type: application
    platform: iOS
    sources:
      - App
    resources:
      - App/Assets.xcassets
    info:
      path: App/Info.plist
      properties:
        CFBundleDisplayName: Odyssey Finance
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        UILaunchScreen:
          UIColorName: LaunchBackground
        NSFaceIDUsageDescription: "Unlock Odyssey Finance with Face ID."
        UIBackgroundModes:
          - processing
        BGTaskSchedulerPermittedIdentifiers:
          - com.uliseis.odysseyfinance.refresh
    entitlements:
      path: App/OdysseyFinance.entitlements
      # Source of truth for capabilities: xcodegen regenerates the .entitlements file
      # from these on every `generate` (a path-only entry wipes it to empty).
      properties:
        com.apple.developer.icloud-services:
          - CloudKit
        com.apple.developer.icloud-container-identifiers:
          - iCloud.com.uliseis.odysseyfinance
        aps-environment: development
    dependencies:
      - package: Core
        product: CoreModel
      - package: Core
        product: CoreLogic
      - package: Core
        product: CoreIntegrations
      - package: Core
        product: CoreSync
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.uliseis.odysseyfinance
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTS_MACCATALYST: NO
        GENERATE_INFOPLIST_FILE: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
